/*Write a program to take string as an input from the user using confirm that the strings are equal.*/
#include <stdio.h>
int main(){
    char str[7];
    scanf("%s",str);
    printf("%s", str);
    return 0;
}